/**
 * IMPORTS
 */

import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%",
    marginTop: 24,
    gap: 12,
  },
  welcomeText: {
    fontSize: 20,
    textAlign: "center",
    margin: 10,
  },
  input: {
    height: 48,
    borderColor: "#DCDCDC",
    borderWidth: 1,
    borderRadius: 8,
    paddingLeft: 16,
    marginBottom: 10,
  },
});

/**
 * EXPORTS
 */
export { styles };
